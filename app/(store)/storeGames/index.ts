import { useGameStore } from '@/app/(store)/storeGames/slices/gameSlice'; // Asegúrate de que la ruta sea correcta
import { fetchGamesAction } from '@/app/(store)/storeGames/actions/gameActions'; // Asegúrate de que la ruta sea correcta

// export { useGameStore, fetchGamesAction };
